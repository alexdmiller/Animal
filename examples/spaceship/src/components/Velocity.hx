package components;

import animal.core.Component;

class Velocity extends Vector {
  public static var ret : ComponentRetriever<Velocity> =
      new ComponentRetriever<Velocity>('components.Velocity');
}